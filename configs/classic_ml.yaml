models: [rf, xgb, svm]
rf:
  n_estimators: 500
xgb:
  n_estimators: 500
  learning_rate: 0.1
  max_depth: 6
svm:
  C_candidates: [0.1, 1.0, 10.0]   # selected on val macro-F1
seed: 42
data:
  norm_mode: none         # features are computed from raw waves; scaler fit on train features
