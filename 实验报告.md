# 实验报告:ITFormer 时序基础模型在焊接质量二分类上的基线研究

- **仓库**:welding-ndt-timeseries(Welding NDT Time Series Foundation Models)
- **报告日期**:2026-08-05(实验状态快照时间:当日 18:35)
- **硬件**:NVIDIA A40(46 GB)×1,单卡顺序/并行训练

---

## 一、项目概述与目标

本项目的长期目标是考察时序基础模型(time-series foundation models)与时序–语言多模态模型在焊接无损检测(NDT)质量评估场景中的适用性。作为第一个实验,我们把 ICML 2025 论文 **ITFormer**(Instruct Time Transformer,arXiv:2506.20093)适配到焊接质量二分类任务上,作为时序基础模型方向的 baseline,并与四族方法做系统对比:

1. **经典机器学习**(随机森林 / XGBoost / SVM + 48 维手工特征);
2. **简单深度学习**(MLP / LSTM / GRU);
3. **近期时序模型**(DLinear / TimesNet);
4. **文献 SOTA 官方复现**(Hahn et al., CIKM 2024 的 VQ-VAE + Transformer 管线)。

ITFormer 原本是一个"时序问答桥接"框架:PatchTST 风格时序编码器 + 可学习指令 token 桥接模块 + 冻结 LLM,用于多模态时序问答。本实验把二分类任务改写为 **QA 式似然打分**(详见第四节),在尽量贴近官方焊接基准协议的前提下,回答两个问题:

- 在单周期焊接质量分类这一小数据、强分布漂移任务上,"编码器 + 桥接 + 冻结大模型"的范式相对传统方法处于什么水平?
- 该基准本身(跨接头类型测试)对所有方法的泛化能力提出了什么程度的挑战?

---

## 二、数据集(Metal Arc Welding / ASIMoW)

**来源**:Zenodo 数据集 "Metal Arc Welding – Predictive Quality Arc Welding Dataset"(Hahn et al.,伍珀塔尔大学,CC-BY-4.0,DOI:10.5281/zenodo.10017718,本实验使用 **v2** 版本)。数据来自 GMAW(熔化极气体保护电弧焊)工艺,焊接电流与电压以 100 kHz 同步采样。

**文件与结构**:核心文件 `processed_asimow_dataset.csv`(约 1.3 GB,MD5 `8245bfaa33d432341a2a9e7d4b4082b5`),每行对应一个焊接周期,列为 `experiment, welding_run, labels, V_0..V_199, I_0..I_199`,即每个周期含 200 个电压采样与 200 个电流采样。标签 `labels`:0 = 质量差,1 = 质量好,-1 = 无标签(全部排除)。

**规模(本仓库预处理实测,与 v2 一致)**:

| 项目 | 数值 |
|---|---|
| CSV 总行数 | 209,185 |
| 有标签行数 | 96,408 |
| 整体 good 占比(有标签) | 0.484 |
| train / val / test 有标签行数 | 74,732 / 10,614 / 11,062 |
| train good 占比 | 0.470 |
| val good 占比 | 0.477 |
| test good 占比 | 0.586 |

**官方划分**按 (experiment, welding_run) 实验对进行(论文 / Zenodo README 惯例),保证同一焊接运行不跨 split 泄漏:

- **val(8 对)**:(3,32), (3,18), (1,27), (3,19), (3,17), (2,21), (1,20), (1,11)
- **test(8 对)**:(3,3), (2,10), (1,24), (3,24), (1,32), (2,1), (1,10), (1,16)
- **train**:其余全部实验对

**有意的分布漂移设定**:测试集对应的工件为 **T 型接头**,而训练/验证集为**搭接接头**——这是基准设计者刻意设置的跨接头类型泛化测试。除信号分布外,标签分布也随之漂移:train 中 good 占 47.0%,而 test 中 good 占 58.6%,多数类发生翻转。多数类基线(在 test 上恒预测 good)的 accuracy 为 0.5859。

**版本差异说明**:2025 年后续 OOD 论文中引用的 48,758 / 4,676 / 104,531 行数属于**更新版数据集**,与本实验所用 v2 不同。CIKM 2024 文献锚点与官方 GitHub repo 均基于 v2,故本仓库全程使用 v2,并以行数断言锁定(见第七节)。

**split 命名陷阱**:官方 repo(`tmdt-buw/VQ-VAE-Transformer-Arc-Welding`)的 `dataloader/utils.py::get_val_test_ids()` 中,两组实验对的**变量名与论文互换**——其 `'test_ids'` 实为论文的 val 对(8 对搭接),其 `'val_ids'` 实为论文的 test 对(T 型接头)。因此官方训练脚本的早停监控实际上运行在论文的 test 集上。本仓库一律以 (exp, run) **对集合 + 行数断言**按论文惯例对齐,不照抄官方变量名;官方 pipeline 在我们环境里跑出的三个 split 行数与我们的 74,732 / 10,614 / 11,062 完全一致,验证了对齐的正确性。

---

## 三、实验协议

**任务定义**:单周期二分类——一个样本 = 一个焊接周期(200 电压 + 200 电流采样),通道顺序固定为 (V, I),与官方实现一致。

**单周期 vs 官方多周期窗口**:官方 repo 的分类模型以连续多周期窗口为样本——raw MLP/GRU 用 `n_cycles=5`(每样本每通道 5×200=1,000 点),VQ-VAE+Transformer 用 `n_cycles=20`(序列长度 `20×(400/patch_size)+1`),而本仓库协议与除官方模型外的所有 baseline 均使用**单周期**(每通道 200 点)。这一协议差异是后文对比官方数字时必须考虑的因素。

**归一化**:逐通道 StandardScaler(global 模式),**只在 train 上 fit**(跨时间步池化,每通道一个 mean/std),再 transform 全部 split——与官方 `MyScaler` 协议一致。实测归一化统计量:电压 21.72 ± 5.22,电流 153.36 ± 172.78(均值 ± 标准差,n_train_cycles = 74,732)。经典 ML 的手工特征从原始信号提取,其特征 scaler 独立地只在 train 特征上 fit。

**训练与评估约定**:

| 项 | 设定 |
|---|---|
| 采样器 | WeightedRandomSampler(权重 1−ratio / ratio,ratio 为 train 中 label 0 占比),与官方一致;损失函数不加权 |
| 优化器 | AdamW + cosine 调度 + 300 步 warmup,梯度裁剪 1.0;LLM 运行使用 bf16 + gradient checkpointing |
| 早停 | val macro-F1,保留 best checkpoint |
| 测试集 | 每个模型/种子**只评一次**,杜绝 test 集调参泄漏 |
| 指标 | accuracy、binary F1(正类 = good)、macro F1、AUC |
| 种子 | 42 / 43 / 44,报 mean±std;经典 ML 与官方 checkpoint 重评固定种子 42 |
| 多数类基线 | test:acc 0.5859 / F1_bin 0.7389 / F1_macro 0.3694 |

所有模型族先经 `smoke_test.sh` 小子集 1 epoch 冒烟验证(能训练、loss 有限且下降、产出 ckpt 与指标、记录峰值显存),再进行全量训练。单元测试覆盖:Qwen3 tokenizer 下 "good"/"bad" 均为单 token(id 18536 / 13855)、占位符恰好 25 个、嵌入替换只改占位行、`requires_grad` 仅限桥接模块、各模块输出形状。

---

## 四、方法

### 4.1 ITFormer 及其 QA 式适配(ITFormer-QA)

ITFormer 原文结构:PatchTST 风格编码器 + 2 层指令感知桥接(将 LIT 可学习指令 token 前置到文本 query 嵌入,经两级 Instruct Time Attention——先通道维、后时间维——融合编码器输出)+ 冻结 LLM;融合出的前 `prefix_num=25` 个 token 替换 prompt 中的占位 token。本仓库的适配(决策 D1)为 **QA 式似然打分**:

- **编码器**:patch_len = stride = 20 → 每通道 10 个 patch;d_model = 512,4 层 8 头(时序自注意力 + 通道注意力 + MLP);输入 `[B,200,2]`,输出 memory `[B,10,2,512]`;
- **ITFormer 桥接**:LIT 为 `nn.Parameter(1,25,512)`;2 层 DecoderBasicBlock,每层先做全序列自注意力 + FFN,再对前 25 个 token 做 Instruct Time Attention(通道维 kv 形状 `[B·10,2,D]` 与时间维 `[B·2,10,D]` 两级注意力,输出相加后投影),得到 `[B,25,512]`;
- **LLM**:本机 Qwen3(论文用 Qwen2.5;按项目要求改用 Qwen3),全冻结 + gradient checkpointing。开发迭代用 Qwen3-1.7B-Base,正式结果用 Qwen3-8B(base,bf16,hidden 4096);
- **Prompt 模板**:`"Welding cycle signals (current and voltage, 200 samples each) are given. <25个占位token> Question: Is this weld good or bad? Answer:"`;桥接输出经 `Linear(512→LLM hidden)` 后替换 25 个占位位置的嵌入(**只改这 25 行**,有单测保证);占位 token 复用 Qwen3 词表中现成的单 token 特殊符号;
- **训练**:只在答案 token 位置计算交叉熵(答案 = good/bad 对应的单 token);
- **评测**:单次 teacher-forced 前向,在预测答案的位置取分数 `score = logit(good) − logit(bad)`,>0 判 good;AUC 直接使用该连续分数。**不做自由生成**——base 版 Qwen3 采样输出为乱码,似然打分是唯一可靠用法;
- **参数规模**:1.7B 实测可训练参数 33.63M / 总参数 1.754B;8B 时投影层随 hidden=4096 变宽,可训练参数约 35.7M,占总参数(约 8.2B)不足 0.5%,与论文"可训练 <1%"的设定一致。

**两个廉价变体**(消融 / sanity 用):

- `itformer_probe`:编码器 + 完整 ITFormer 桥接 + 池化分类头,**无 LLM**;
- `encoder_only`:仅编码器 + 分类头。

### 4.2 Baseline 家族

| 家族 | 模型与要点 |
|---|---|
| 经典 ML | 48 维手工特征:每通道时域统计(均值/方差/RMS/峰峰/偏度/峭度/分位数/crest/过零率/差分统计等)+ FFT 谱特征(带能量/谱质心/谱熵)+ 通道间 Pearson 相关与 RMS 比。RF(500 树)、XGBoost(500 树,lr 0.1,depth 6)、SVM(RBF,C ∈ {0.1, 1, 10} 按 val macro-F1 选择;AUC 用 `decision_function` 打分) |
| 简单深度 | MLP(400→256→128→2);2 层 LSTM / GRU(hidden 128,输入逐时间步 2 维) |
| 近期时序 | DLinear(moving-average kernel 25);TimesNet(top_k 3,d_model 64,2 层)+ 分类头 |
| 官方 SOTA | 见 4.3 |

以上模型统一使用 global 归一化、WeightedRandomSampler、val macro-F1 早停、种子 42/43/44。

### 4.3 官方 SOTA 复现方案(Hahn et al., CIKM 2024)

文献锚点:**VQ-VAE + Transformer ≈ 79.7% acc / ≈77% F1**。复现方式:

1. 建立专用 conda 环境 `vqvae-welding`(Python 3.11 + torch 2.5.1 cu121 + lightning 2.4.0 + torchmetrics 1.4.1 + vector-quantize-pytorch 1.14.24 等,较官方 environment.yaml 更轻量的 pip-only 安装);
2. 将数据 CSV 软链进官方 `data/`,按官方脚本原样训练:VQ-VAE-Patch(重构模型)→ MLP/GRU(raw,n_cycles=5)→ VQ-VAE-MLP/GRU(潜空间)→ VQ-VAE-Transformer(n_cycles=20);
3. 训练完成后用 `scripts/eval_official_ckpt.py` 在**官方环境与官方模型代码**内重新推理,仅把 split 对替换为我们的 canonical(论文惯例)val/test,重算全部指标。

---

## 五、当前结果

### 5.1 主对比表(test 集,截至 2026-08-05 18:35)

下表汇总自 `experiments/results/*.json`(冒烟 run 已剔除);3 种子模型报 mean±std(样本标准差);official 行为官方 checkpoint 在 canonical split 上的重评值(单种子);wall 为单种子训练墙钟时间(官方行为其原训练时间,未记录)。

| 模型 | 种子 | test acc | test F1(二值) | test F1(macro) | test AUC | 参数量(M) | wall(s) |
|---|---|---|---|---|---|---|---|
| official_mlp(官方,5 周期窗口) | 1 | **0.7150** | 0.7515 | **0.7088** | 0.7357 | – | – |
| classic_rf(48 维特征) | 1 | 0.7029 | **0.7547** | 0.6891 | **0.8038** | – | 10.9 |
| classic_xgb(48 维特征) | 1 | 0.7020 | 0.7500 | 0.6905 | 0.7869 | – | 2.0 |
| official_gru(官方,5 周期窗口) | 1 | 0.6713 | 0.7418 | 0.6449 | 0.7367 | – | – |
| dlinear | 3 | 0.6657±0.0234 | 0.7115±0.0182 | 0.6557±0.0290 | 0.6783±0.0134 | 0.2 | 22.9 |
| classic_svm(48 维特征) | 1 | 0.6633 | 0.7105 | 0.6540 | 0.7596 | – | 735.6 |
| official_vqvae_gru(官方潜空间) | 1 | 0.6623 | 0.7390 | 0.6304 | 0.6754 | – | – |
| itformer_probe(桥接+分类头,无 LLM) | 3 | 0.6451±0.0244 | 0.7070±0.0338 | 0.6073±0.0762 | 0.7019±0.0154 | 31.5 | 312.2 |
| official_vqvae_mlp(官方潜空间) | 1 | 0.6448 | 0.7228 | 0.6143 | 0.6753 | – | – |
| encoder_only(仅编码器) | 3 | 0.6432±0.0194 | 0.6967±0.0076 | 0.6285±0.0333 | 0.7235±0.0116 | 16.8 | 168.0 |
| mlp | 3 | 0.6329±0.0086 | 0.7199±0.0053 | 0.5929±0.0194 | 0.6698±0.0046 | 0.1 | 11.3 |
| timesnet | 3 | 0.6169±0.0426 | 0.6845±0.0308 | 0.5980±0.0486 | 0.6755±0.0474 | 4.7 | 528.6 |
| gru | 3 | 0.6166±0.0102 | 0.7302±0.0123 | 0.5332±0.0094 | 0.6857±0.0128 | 0.2 | 31.8 |
| lstm | 3 | 0.6131±0.0076 | 0.7213±0.0198 | 0.5398±0.0254 | 0.6918±0.0094 | 0.2 | 34.2 |
| 多数类基线 | – | 0.5859 | 0.7389 | 0.3694 | – | – | – |
| itformer_qa(Qwen3-8B) | 3 | 进行中 | 进行中 | 进行中 | 进行中 | ≈8,200(可训练 ≈35.7) | – |
| itformer_qa(Qwen3-1.7B) | 3 | 排队中(8B 之后自动启动) | – | – | – | 1,754(可训练 33.6) | – |

**1.7B ITFormer-QA 学习率探针**(单 epoch、seed 42,仅用于选学习率,非正式结果;据此选定 lr = 5e-5 用于 8B 与 1.7B 正式运行):

| lr | val acc | val macro-F1 | val AUC | test acc | test macro-F1 | test AUC | VRAM 峰值 |
|---|---|---|---|---|---|---|---|
| 1e-5 | 0.7595 | 0.7564 | 0.8961 | 0.6456 | 0.6455 | 0.6674 | 5.15 GB |
| **5e-5** | **0.7742** | **0.7640** | **0.9057** | 0.6370 | 0.6352 | 0.6772 | 5.15 GB |
| 1e-4 | 0.7623 | 0.7485 | 0.8923 | 0.6424 | 0.6403 | 0.6891 | 5.15 GB |

### 5.2 结果解读

**(1)经典 ML 目前最强,且优势主要来自特征而非模型容量。** RF 与 XGBoost 在 48 维手工特征上取得 test acc ≈0.70、AUC 0.79–0.80,是所有方法中最好。原因分析:谱能量、互相关等手工特征与焊接质量的物理机制(电弧稳定性、热输入)直接对应,对接头类型变化相对不变;树模型在 7.5 万样本规模下不易过拟合。与之对照,所有端到端深度模型的 val acc 达 0.79–0.83,test 却只有 0.57–0.68,说明它们更多记住了搭接接头的分布细节。

**(2)分布漂移是本基准的核心难度。** 每个模型都出现显著的 val→test 跌落;test 集 good 占比(0.586)高于 train(0.470),多数类发生翻转,导致模型普遍偏向预测 good——这解释了为何各模型 binary F1(正类 good)普遍高于 macro F1。在这种设定下,绝对准确率的意义有限,跨模型比较更应关注 AUC 与 macro-F1。

**(3)probe / encoder-only 消融:桥接本身(无 LLM)收益不明显。** `itformer_probe`(31.5M)相对 `encoder_only`(16.8M)参数量接近翻倍,但 test acc 仅 +0.2pt(0.6451 vs 0.6432),macro-F1 与 AUC 反而略低(0.6073 vs 0.6285;0.7019 vs 0.7235)。即在单周期小输入、无 LLM 的条件下,两级指令注意力桥接没有带来清晰收益;两者也都低于经典 ML。"编码器 + 桥接 + 冻结 LLM"完整范式的价值取决于 8B QA 结果(进行中)。

**(4)官方模型复现低于文献锚点,原因有多重。** canonical 重评下官方模型 test acc 为 0.64–0.72,全部低于 CIKM 2024 论文的 ≈79.7% acc / 77% F1 锚点。差距的可能来源:(a)**split 变量名互换**——官方训练以"论文的 test 集"做早停与 checkpoint 选择,论文报告数字、发布 checkpoint 与我们重评所用集合之间的对应关系存在歧义;(b)官方 checkpoint 按 `val/loss` 最小选择,与论文报告的 F1 指标不一致;(c)**窗口协议差异**——官方模型每样本看到 5–20 个连续周期,我们的单周期 baseline 信息量更少;(d)数据集版本差异的可能影响。官方 VQ-VAE+Transformer 的重评仍在进行中(第六节),将给出与文献最可比的锚点。值得注意的是,官方模型的 val→test 差距明显小于我们的单周期深度模型(如 official_mlp:0.7475→0.7150),提示多周期上下文对接头类型漂移更稳健。

**(5)ITFormer-QA 的初步信号。** 1.7B 仅训练 1 epoch(lr 5e-5)即达 val macro-F1 0.7640 / AUC 0.9057,已接近 probe/encoder_only 完整训练后的 val 水平(0.79–0.82 区间下沿),说明冻结 LLM + 桥接的范式能够有效提取判别信息;但 test acc 仅 0.637,泛化同样受分布漂移制约。8B 正式结果(3 种子)正在训练,见第六节。

---

## 六、正在进行的实验与后续计划(截至 2026-08-05 18:35)

1. **ITFormer-QA 8B × 3 种子(seed 42/43/44)**:seed 42 自当日 14:27 开始训练(已运行约 4 小时),配置 lr = 5e-5、batch 16×2 累积(有效 batch 32)、至多 5 epoch、val macro-F1 早停 patience 2。nvidia-smi 显示该进程占用显存约 17.8 GB、GPU 100% 利用。截至快照时间 `experiments/results/itformer_qa_qwen3-8b_seed42.json` 尚未生成;seed 42 完成后将顺序运行 43、44。
2. **ITFormer-QA 1.7B × 3 种子**:已排队。等待脚本监视到 8B 批次结束后自动启动 `run_itformer_qa.sh full 1p7b`(lr 5e-5、batch 32、至多 5 epoch)。
3. **官方 VQ-VAE+Transformer**:当日 16:09 启动(此前 VQ-VAE-Patch 重构模型已训完:val/loss 早停 patience 5,于第 45 epoch 触发停止(上限 50),test/recon_error 0.0526)。Transformer 训练脚本为 3 轮"生成阶段(至多 10 epoch)+ 分类阶段"交替,末轮以 10 epoch 分类微调收尾(monitor `val/cl/f1_score`,patience 5)。快照时刻处于**第 1 轮生成阶段 Epoch 3/10**,约 5.3 it/s(10,162 步/epoch,约 32 min/epoch);训练完成后将以 `eval_official_ckpt.py` 在 canonical split 上重评。
4. **GPU 共享**:上述两个任务同卡并行,A40 显存占用约 23.2/46 GB,双方吞吐均低于单独运行(这也是 8B 单种子耗时长于早期估算的原因)。
5. **后续收尾**:全部结果就绪后,`python scripts/make_table.py` 重新生成汇总表;更新 README(结果表 + 复现命令);本报告随之定稿。备用方案:若 8B 的 val macro-F1 < 0.60,将按预案追加归一化消融(norm_mode = cycle_z / none,global 为默认)。

---

## 七、工程与可复现性

### 目录结构

```
welding-ndt-timeseries/
  configs/       每个模型族一份 YAML(data.yaml; itformer_qa_qwen3_{8b,1p7b}.yaml;
                 itformer_probe.yaml; encoder_only.yaml; simple_dl.yaml;
                 modern_ts.yaml; classic_ml.yaml)
  data/          raw/ 原始 CSV;processed/ memmap(waves.npy N×2×200 float32、
                 meta_*.npy、norm_stats.json、splits/{train,val,test}_idx.npy)(不入 git)
  src/wndt/      包:data(下载/预处理/split/dataset)、models(encoder、itformer、
                 time_language_model、simple_dl、dlinear、timesnet、heads)、
                 features/handcrafted.py、train、eval、utils
  scripts/       01_download_data.sh、02_preprocess.py、train.py、run_classic_ml.py、
                 run_baselines.sh、run_itformer_qa.sh、setup_official_env.sh、
                 run_official_repo.sh、eval_official_ckpt.py、smoke_test.sh、make_table.py
  tests/         test_models.py(形状/占位替换/梯度隔离等单测,可加 --with-llm)
  experiments/   runs/(ckpt 与日志)+ results/*.json + comparison_table.{md,csv}
  third_party/   官方 tmdt-buw repo clone(仅一处必要补丁,见下)
```

### 复现命令

```bash
bash scripts/01_download_data.sh           # Zenodo CSV 下载 + MD5 门槛
python scripts/02_preprocess.py            # CSV→memmap、canonical split、归一化统计
python tests/test_models.py --with-llm     # 单测(GPU,Qwen3-1.7B)
bash scripts/smoke_test.sh                 # 各模型族 1-epoch 小子集冒烟
bash scripts/run_baselines.sh              # 我方 baseline,种子 42/43/44
python scripts/run_classic_ml.py           # RF/XGBoost/SVM
bash scripts/setup_official_env.sh         # vqvae-welding conda 环境
bash scripts/run_official_repo.sh          # 官方管线复现
python scripts/eval_official_ckpt.py ...   # canonical split 重评官方 ckpt
bash scripts/run_itformer_qa.sh sweep      # 1.7B 单 epoch lr 探针
bash scripts/run_itformer_qa.sh full 8b    # 8B 正式运行(3 种子)
python scripts/make_table.py               # 汇总 → comparison_table.{md,csv}
```

### 已踩的坑与修复

1. **官方 split 变量名互换**:`get_val_test_ids()` 的 `'test_ids'`/`'val_ids'` 与论文惯例互换(见第二节)。对策:一律按 (exp, run) 对集合并以精确行数断言(74,732 / 10,614 / 11,062)对齐;官方 pipeline 在本环境产出的行数与我们完全一致。
2. **预处理归一化统计的 reshape bug**:逐通道 mean/std 必须先把 train 数据 reshape 成 channel-last `(N×200, 2)` 再池化,否则两通道统计量互相污染(已修复,`norm_stats.json` 数值即修复后结果)。
3. **SVM 打分**:`probability=True`(Platt 校准)的代价约等于多训一遍 SVM,改用 `decision_function` 输出计算 AUC;C 在 {0.1, 1, 10} 中按 val macro-F1 内选。
4. **官方脚本两处问题**:(a)`train_classification_model.py` 的 `--logging-tag` 无默认值,而 `main()` 入口无条件执行 `logging_tag.split(",")`,缺省会立刻抛 AttributeError——复现运行时必须显式传 `--logging-tag`(本次复现传 `repro`,见运行日志 hparams);(b)`train_transformer_mtasks.py::classification_finetuning()` 中的 `ModelCheckpoint` 回调被上游注释掉,不解开则训练结束不会留下任何 Transformer 权重,我们将其解注释并补进 callbacks 列表——这是 `third_party/` 中唯一的代码补丁(git diff +5/−3,代码内有注释说明)。
5. **窗口协议差异**:官方 raw MLP/GRU 用 `n_cycles=5`、Transformer 用 `n_cycles=20`,均为多周期窗口;我们的单周期协议与之对比时须注明(见第五节解读 (4))。

### 验证记录

- 数据:MD5 校验通过;split 行数断言 74,732 / 10,614 / 11,062;(exp, run) 对跨 split 互斥;label = -1 全部排除;memmap 随机抽 5 行与 CSV 逐位一致。
- 模型单测:good/bad 单 token id 18536 / 13855;占位符恰好 25 个;嵌入替换只作用于占位行;`requires_grad` 仅限桥接(1.7B 下 33.63M / 1.754B);编码器 `[B,2,10,512]` → ITFormer `[B,25,512]` 形状断言通过。
- 冒烟:8 个模型族全部通过(期间修复 CUDA→numpy 转换、smoke 子集单类别两个 bug)。

---

## 八、参考文献与资源

1. **数据集**:Hahn, S., et al. *Metal Arc Welding – Predictive Quality Arc Welding Dataset*. Zenodo, DOI: [10.5281/zenodo.10017718](https://doi.org/10.5281/zenodo.10017718)(CC-BY-4.0,v2):https://zenodo.org/records/10017718
2. **基准论文**:Hahn, S., et al. *Quality Prediction in Arc Welding: Leveraging Transformer Models and Discrete Representations from Vector Quantised-VAE*. CIKM 2024, DOI: [10.1145/3627673.3680031](https://doi.org/10.1145/3627673.3680031);官方代码:https://github.com/tmdt-buw/VQ-VAE-Transformer-Arc-Welding
3. **模型论文**:Wang, Y., et al. *ITFormer: Bridging Time Series and Natural Language for Multi-Modal QA with Large-Scale Multitask Dataset*. ICML 2025, [arXiv:2506.20093](https://arxiv.org/abs/2506.20093);官方代码:https://github.com/Pandalin98/ITFormer-ICML25
4. **LLM**:Qwen3-8B / Qwen3-1.7B-Base(本地权重,bf16)
