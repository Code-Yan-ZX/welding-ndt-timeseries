[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "wndt"
version = "0.1.0"
description = "Welding NDT Time Series: ITFormer baseline on the Metal Arc Welding dataset"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
