# data artifacts
data/raw/
data/processed/

# experiment artifacts (results/*.json + tables are committed)
experiments/runs/
experiments/results/**/*.pt
experiments/results/**/*.ckpt

# third party clones
third_party/

# local model weights (symlink or download Qwen3 here) - root only, not src/wndt/models/
/models/

# python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/

# misc
*.log
.DS_Store

# local agent memory (contains secrets — never commit)
CLAUDE.md

# P2 multimodal LLM venv (vLLM/torch, 不提交)
.venv_p2/

# M0-2C EddyCus 全量审计转储（大文件，不入库）
experiments/results/m0_2c_eddycus_audit_full.json
